{{- if .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "web-app-chart.fullname" . }}-config
  labels:
    {{- include "web-app-chart.labels" . | nindent 4 }}
data:
  index.html: |
{{ .Values.configMap.indexHtml | indent 4 }}
{{- end }}

apiVersion: v1
kind: Service
metadata:
  name: multi-nodeport
spec:
  selector:
    app: multi-app
  ports:
    - name: nginx
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
